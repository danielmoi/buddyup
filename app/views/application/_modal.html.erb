      <%= link_to "BuddyUP", "", {:goal_id => goal.id, :initiator => goal.initiator.name, :goal => goal.title.titleize, :method => 'POST', :class => 'btn btn__buddy-up--confirm' } %> with <%= goal.initiator.name %> to <%= goal.title %>!
