<h1>All Categories</h1>

<%= link_to 'Create Category', new_category_path, :class => 'btn' %>

<% @categories.each do |category| %>
  <p>
    <%= link_to category.title, category_path(category) %>
  </p>
<% end %>
